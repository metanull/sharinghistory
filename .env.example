# Copy this file to .env and paste your own GitHub token (classic, with the
# read:packages scope). It only lets your computer download this website's
# building blocks from GitHub Packages. It is personal: never share it, and
# never commit the .env file (it is gitignored).
NODE_AUTH_TOKEN=
